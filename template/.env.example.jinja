# Copy to .env and adjust. Consumed by docker-compose.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB={{ db_name }}
POSTGRES_PORT=5432
BACKEND_PORT=8080
{%- if caching == 'redis' %}
REDIS_PORT=6379
{%- endif %}
